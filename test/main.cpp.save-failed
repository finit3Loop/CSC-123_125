#include <iostream>
#include <cmath>

using namespace std;
 int lowestPosition(double a[], int size);
int main()
{
  double  a[1000];
  lowestPosition(a[],1000);


//function call
cout << (9-4)/2;
    return 0;
}


int lowestPosition(a[],size)
{
    int position(0);
    for(int i(0);i<size;i++)
    {
        if(a[i]<a[lowestPosition])
        {
         position=i;
        }
    }


return position;

}



